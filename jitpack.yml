jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.12-open
  - sdk use java 17.0.12-open

install:
  - ./gradlew :tantivy-android:assembleRelease
  - mkdir -p build/outputs/aar
  - cp tantivy-android/build/outputs/aar/tantivy-android-release.aar build/outputs/aar/
